import React from 'react';

const FooterText = () => (
    <React.Fragment>
        &copy; {new Date().getFullYear()} ParkingPro. Đã đăng ký bản quyền.
    </React.Fragment>
);

export { FooterText };
